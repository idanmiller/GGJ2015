DecisionDialog = function(game, config, resource) {
    Dialog.call(this, game, config, "dialog");
};

DecisionDialog.prototype = Object.create(Dialog.prototype);
DecisionDialog.prototype.constructor = DecisionDialog;

